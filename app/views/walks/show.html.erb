<div class="flex justify-center">
  <div class="lg:w-5/6 lg:m-5 p-3">
    <div class="m-2">
      <div class="flex justify-center p-3">
        <%= cl_image_tag("walking_c2qkol", class:"w-16 h-16") %>
        <div class="mx-2">
          <h1 class="font-headings font-bold text-md">Feeling <%= (@walk.therapy.mood.name).downcase %>? Wanna go for a <%= @walk.therapy.sub_category %>?</h1>
          <h1 class="font-headings text-base my-2">Here's a route for you to walk it out</h1>
        </div>
      </div>
      <div id="map" class="my-4"
     style="width: 100%; height: 600px;"
     data-markers="<%= @markers.to_json %>"
     data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
      </div>
    </div>
  </div>
</div>

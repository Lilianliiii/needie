 <div class="flex justify-center">
  <div class="lg:w-1/2 m-5 shadow rounded-3xl">
    <div class="flex justify-center">
      <div class="p-3">
        <%= cl_image_tag("affirmations_or7j1g", class:"w-24 h-24") %>
      </div>
      <div class="justify-center mt-5">
        <h1 class="font-headings text-lg">Repeat the following:</h1>
      </div>
    </div>
    <div class="flex justify-center p-5 font-headings text-neediegreen">
      <% @therapies.each do |therapy| %>
      <%= therapy.content.text %>
      <% end %>
    </div>
  </div>
</div>

<div class="flex justify-center">
  <div class="lg:w-5/6 lg:m-5 shadow rounded-3xl p-3">
    <% if @animation.therapy.sub_category == 'joke of the day' %>
      <div class="flex justify-center p-3">
        <%= cl_image_tag("joke_lcmg27", class:"w-20 h-20") %>
        <div class="m-3">
          <h1 class="font-headings font-bold lg:text-lg text-sm">So you're feeling <%= (@animation.therapy.mood.name).downcase %> and you want to have a laugh?</h1>
          <h1 class="font-headings lg:text-lg text-sm">Check out this joke!</h1>
        </div>
      </div>
    <% end %>
      <div class="flex justify-center p-5 font-headings text-neediegreen">
        <%= @animation.link %>
      </div>
  </div>
</div>
